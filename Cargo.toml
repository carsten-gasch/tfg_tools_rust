[package]
name = "tfg_tools_rust"
version = "0.1.0"
edition = "2024"
authors = ["Carsten Gasch"]
description = "A set of tools to make tedious and repetitive work faster."

[dependencies]
anyhow = "1.0.100"
clap = { version = "4.5.56", features = ["derive"] }
regex = "1.12.2"
